name: Deploy Wiki Site

on:
  push:
    branches: [main]
    paths:
      - 'wiki/**'
      - 'app/src/main/assets/data/**'
      - 'app/src/main/res/drawable/**'
      - 'app/src/main/res/values/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r wiki/requirements.txt

      # Markdown wiki output is only pushed to tristinbaker/IdleFantasy's GitHub Wiki.
      # This fork has no *.wiki repo, so skip to avoid clone failures.
      - name: Generate wiki pages
        if: github.repository == 'tristinbaker/IdleFantasy'
        run: python -m wiki.src write

      - name: Generate HTML site
        run: python -m wiki.src write-html

      - name: Update GitHub Wiki
        if: github.repository == 'tristinbaker/IdleFantasy'
        uses: Andrew-Chen-Wang/github-wiki-action@v4
        with:
          path: out/IdleFantasy-wiki
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy to GitHub Pages (upstream)
        if: github.repository == 'tristinbaker/IdleFantasy'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: out/IdleFantasy-site
          cname: idlefantasy.tristinbaker.xyz
          keep_files: true

      - name: Deploy to GitHub Pages (fork)
        if: github.repository != 'tristinbaker/IdleFantasy'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: out/IdleFantasy-site
          keep_files: true
